# MySQL 스키마 먼저 생성: CREATE DATABASE s2t CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
DATABASE_URL=mysql+aiomysql://root:0000@127.0.0.1:3306/s2t
# faster-whisper 모델 크기: tiny, base, small, medium, large-v3 등 (GPU 있으면 medium도 가능)
WHISPER_MODEL=base
